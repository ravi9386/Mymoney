node_modules
# `dist/` is a stray Vite default — we use `docs/` for the build output
# (committed so GitHub Pages can serve it from main/docs).
dist
dist-ssr
*.local
.vite
.DS_Store
*.tsbuildinfo
.env
.env.*
!.env.example
.idea
.vscode
*.log
