node_modules
# `dist/` is the Vite build output. We do NOT ignore the built artifacts at
# the repo root (index.html, 404.html, assets/, favicon.svg) — those are
# committed so GitHub Pages can serve the app at vermawisdom.com.
dist
dist-ssr
*.local
.vite
.DS_Store
*.tsbuildinfo
.env
.env.*
!.env.example
.idea
.vscode
*.log
